<div class="line">
	<div class="headingline">Thư viện ảnh & Video</div>
	<div class="home-gallery-video">
		<div class="sp-image-container">
			<?php if(sizeof($arrImg) > 0): ?>
				<h2 class="title-path-ext"><a href="<?php echo URL::route('site.pageLibrary'); ?>" title="Thư viện ảnh">Thư viện ảnh</a></h2>
				<div id="sliderPro" class="slider-pro">
					<?php foreach($arrImg as $k=>$items): ?>
                        <?php $arrListImg = ($items->image_image_other != '') ? unserialize($items->image_image_other) : array();?>
						<?php if(!empty($arrListImg)): ?>
							<div class="sp-slides">
								<?php foreach($arrListImg as $item): ?>
									<div class="sp-slide">
										<img class="sp-image" src="<?php echo URL::route('site.home'); ?>/assets/frontend/img/blank.gif"
											 data-src="<?php echo ThumbImg::getImageThumb(CGlobal::FOLDER_LIBRARY_IMAGE, $items['image_id'], $item, CGlobal::sizeImage_1000); ?>"
											 data-small="<?php echo ThumbImg::getImageThumb(CGlobal::FOLDER_LIBRARY_IMAGE, $items['image_id'], $item, CGlobal::sizeImage_1000); ?>"
											 data-medium="<?php echo ThumbImg::getImageThumb(CGlobal::FOLDER_LIBRARY_IMAGE, $items['image_id'], $item, CGlobal::sizeImage_1000); ?>"
											 data-large="<?php echo ThumbImg::getImageThumb(CGlobal::FOLDER_LIBRARY_IMAGE, $items['image_id'], $item, CGlobal::sizeImage_1000); ?>"
											 data-retina="<?php echo ThumbImg::getImageThumb(CGlobal::FOLDER_LIBRARY_IMAGE, $items['image_id'], $item, CGlobal::sizeImage_1000); ?>"/>
										<p class="sp-layer sp-white sp-padding"
										   data-horizontal="50" data-vertical="50"
										   data-show-transition="left" data-show-delay="400"><?php echo $items['image_title']; ?>

										</p>
									</div>
								<?php endforeach; ?>
							</div>
							<div class="sp-thumbnails">
								<?php foreach($arrListImg as $item): ?>
									<img class="sp-thumbnail" src="<?php echo ThumbImg::getImageThumb(CGlobal::FOLDER_LIBRARY_IMAGE, $items['image_id'], $item, CGlobal::sizeImage_500); ?>"/>
								<?php endforeach; ?>
							</div>
						<?php endif; ?>
					<?php endforeach; ?>
				</div>
				<script type="text/javascript">
                    $( document ).ready(function( $ ) {
                        $( '#sliderPro' ).sliderPro({
                            width: 570,
                            height: 265,
                            fade: true,
                            arrows: true,
                            buttons: false,
                            fullScreen: true,
                            shuffle: true,
                            smallSize: 500,
                            mediumSize: 1000,
                            largeSize: 3000,
                            thumbnailArrows: true,
                            autoplay: false
                        });
                    });
				</script>
			<?php endif; ?>
		</div>
		<div class="home-video">
			<h2 class="title-path-ext"><a href="<?php echo URL::route('site.pageVideo'); ?>" title="Video">Video</a></h2>
			<?php if(sizeof($arrVideo) > 0): ?>
				<?php foreach($arrVideo as $k=>$item): ?>
					<?php if($k == 0): ?>
                        <?php
                        $_video = str_replace('https://www.youtube.com/watch?v=', 'https://www.youtube.com/embed/', $item->video_link);
                        $embed = '<iframe width="540" height="350" src="'.$_video.'?rel=0" frameborder="0" allowfullscreen></iframe>';
                        echo $embed;
                        ?>
					<?php endif; ?>
				<?php endforeach; ?>
			<?php endif; ?>
		</div>
	</div>
</div>